/** Canonical UNM wordmark (SVG embeds raster — use surface blend on nav/footer). */
export const LOGO_SVG = '/logo-unm.svg';

export const LOGO_ALT = 'Université Numérique du Maroc';

/** Matches `.glass-nav` canvas tone — removes white matte on light surfaces. */
export const LOGO_BG_LIGHT = 'rgb(255, 252, 249)';

/** Matches `.glass-footer` chocolate glass — removes white matte on dark surfaces. */
export const LOGO_BG_DARK = 'rgb(35, 14, 6)';
