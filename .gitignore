# ───── Dependencies & lock store ──────────────────────────────
node_modules
.pnpm-store
package-lock.json
yarn.lock

# ───── Build artifacts ────────────────────────────────────────
.next
.turbo
dist
build
out
apps/cms/build
apps/cms/dist

# ───── Environment files (SECRETS — never commit) ─────────────
.env
.env.local
.env.development.local
.env.production.local
.env.test.local
.env.*.local

# ───── User uploads (CMS media) — production-only data ────────
apps/cms/media

# ───── Logs & coverage ────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
coverage
.nyc_output

# ───── IDE / OS ───────────────────────────────────────────────
.DS_Store
Thumbs.db
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
.idea
*.swp
*.swo

# ───── Vercel / Next ──────────────────────────────────────────
.vercel
next-env.d.ts

# ───── Tarballs / archives ────────────────────────────────────
*.tar.gz
*.zip
unm-website-*.tar.gz

# ───── Smoke test temp files ──────────────────────────────────
/tmp/smoke.body
